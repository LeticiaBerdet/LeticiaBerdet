<%-include('cabecalho.ejs')%>
    
    <form method="POST">
        <input type="text" name="Pesquisa" placeholder="Pesquisar">
        <input type="submit">
    </form>
    <table>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Endereço</th>
            <th>Cell</th>
            <th>Edita</th>
            <th>Deleta</th>
        </tr>
        <% listaAlunos.forEach(function(aluno){%>
        <tr>
            <td><%=aluno._id%></td>
            <td><%=aluno.nome%></td>
            <td><%=aluno.endereco%></td>
            <td><%=aluno.cell%></td>
            <td style="text-align: center;">
                <a href="/edit/<%=aluno._id%>">
                    <img src="editar.png" style="width: 20px" alt="icone de editar"/>
                </a>
            </td>
            <td style="text-align: center;">
                <a href="/del/<%=aluno._id%>">
                    <img src="lixo.png" style="width: 30px", alt="icone de deletar"/>
                </a>
            </td>
            
        </tr>
    <% }) %>
    </table>
    <h2><%=msg%></h2>
    <%-include('rodape.ejs')%>
